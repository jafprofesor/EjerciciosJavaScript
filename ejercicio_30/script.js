// Código base para el ejercicio 30
const elemento = document.getElementById("miElemento");
const boton = document.getElementById("miBoton");

boton.addEventListener("click", () => {
  /* EJERCICIO 30:
     Al hacer clic más de 3 veces en el botón, cambiar el color
     del texto del elemento a rojo permanentemente */
});
