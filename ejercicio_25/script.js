// Código base para el ejercicio 25
const elemento = document.getElementById('miElemento');
const boton = document.getElementById('miBoton');

boton.addEventListener('click', () => {
  /* EJERCICIO 25:
     Al hacer clic en el botón, alternar la visibilidad del elemento
     usando la propiedad CSS 'display' (none/block) */
});